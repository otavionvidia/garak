pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,tsx}"
      run: yarn lint {staged_files} --max-warnings=0
    format:
      glob: "*.{ts,tsx,json,css,md}"
      run: yarn format --check {staged_files}
    typecheck:
      run: yarn check
